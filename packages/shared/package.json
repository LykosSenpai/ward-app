{
  "name": "@ward/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "check": "tsc --noEmit --project tsconfig.json --pretty false",
    "build": "tsc --noEmit --project tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
